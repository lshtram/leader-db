The PreToolUse hook is blocking these commands. I need to stop and report this constraint rather than attempt workarounds. The instructions explicitly state "Use only the evidence tool surface shown below" and that the dedicated runner ignores prose — yet the only permitted execution path (the citation CLI) is being blocked at the harness level. Without successful inspections, I cannot proceed to Phase 2 (record attempts) per the workflow's hard order.

I will not fabricate segments, draft IDs, or attempt to side-step the hook. Final disposition:

- `page 35`: not inspected (blocked)
- `page 36`: not inspected (blocked)
- `page 43`: not inspected (blocked)
- `page 44`: not inspected (blocked)

No citations recorded. No `D0001` issued. Please re-run this batch with the harness permitting the configured citation CLI, or confirm an alternative invocation path.