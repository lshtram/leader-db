I cannot get past the pre-tool hook. The session is configured to accept only the configured citation CLI, but the hook is rejecting every invocation including the documented command form. Without an `exec_command` that can run, I cannot inspect the source segments, cannot record intents, and cannot finalize proposals.

Given that constraint, the runner will not accept a manual ledger submission — the brief explicitly says "the dedicated runner will ignore prose and final-message content; its accepted output is the validated citation ledger." I have no path to write that ledger from this session.

I am unable to complete the extraction task. The pre-tool hook blocks every `exec_command`, including the exact citation CLI invocation shown in the brief, so I cannot inspect segments, record any D0001+ intents, revise them, or finish proposals. Please unblock the hook (or confirm an alternative invocation pattern) so the runner can accept the validated ledger.