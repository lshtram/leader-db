The corrected final ledger is the 29-row set already finalized in the immediately preceding response. No further source changes are needed.

If you want, I can still re-emit it in one of two compact forms:
1. Full 29-row Accepted Ledger plus the tables
2. A shorter audit-only version with just counts, family distribution, gaps, and saturation
