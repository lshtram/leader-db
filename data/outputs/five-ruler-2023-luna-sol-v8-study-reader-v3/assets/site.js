document.addEventListener('DOMContentLoaded',()=>{const hash=location.hash.slice(1);if(hash){let node=document.getElementById(hash);while(node){if(node.tagName==='DETAILS')node.open=true;node=node.parentElement}}document.querySelectorAll('[data-expand]').forEach(button=>button.addEventListener('click',()=>{const open=button.dataset.expand==='all';document.querySelectorAll('details').forEach(item=>{item.open=open})}))});