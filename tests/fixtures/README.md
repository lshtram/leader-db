# fixtures/

Tiny sample fixtures used by the test suite. Files here are intentionally
minimal so a test failure is easy to diagnose.

During Phase D (testing) the suite grows with small xlsx/docx fixtures
that exercise the Stage 1 client matrix parser and the Stage 2 per-source
adapters. The fixtures stay committed so tests are reproducible without
network access.
